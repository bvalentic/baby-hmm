# Parking Lot

Markdown parking lot for miscellaneous code as more features are onboarded

## Copied code

```python
print(full_df.columns) 
print(full_df.isna().sum()) # This will tell you exactly which column is causing the rows to be dropped
```

## Pasting code

```python

```
